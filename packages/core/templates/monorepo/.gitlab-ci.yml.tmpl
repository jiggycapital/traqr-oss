# Generated by Traqr — do not edit directly
# Regenerate with: npx traqr render --force
#
# GitLab CI/CD pipeline for Traqr-managed monorepo
# Runs on merge requests only (no wasted pipeline minutes)

stages:
  - validate
  - build

# ─── Validate ─────────────────────────────────────────────
# Fast checks: dependencies, template rendering, skill validation

validate:
  stage: validate
  image: node:20-alpine
  script:
    - npm ci --ignore-scripts
    - npm run build:packages
    - npx traqr render --dry-run
  rules:
    - if: $CI_MERGE_REQUEST_IID
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/

# ─── Build ────────────────────────────────────────────────
# Full build: packages first, then apps

build:
  stage: build
  image: node:20-alpine
  script:
    - npm ci --ignore-scripts
    - npm run build
  rules:
    - if: $CI_MERGE_REQUEST_IID
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/
  artifacts:
    paths:
      - packages/*/dist/
    expire_in: 1 hour

'use client'

{{#IF_DESIGN_DARK_MODE}}
import { useEffect } from 'react'
{{/IF_DESIGN_DARK_MODE}}
import { ToastProvider } from './ui/Toast'

/**
 * Providers — Client-side provider wrapper
 * Generated by Traqr Init ({{DESIGN_FLAVOR}} flavor). Edit freely.
 *
 * Wrap your app root with this component in layout.tsx:
 *   <Providers>{children}</Providers>
 *
 * TODO: Add your own providers (auth, query client, etc.) here.
 */

export function Providers({ children }: { children: React.ReactNode }) {
{{#IF_DESIGN_DARK_MODE}}
  useEffect(() => {
    // Restore dark mode preference from localStorage
    const stored = localStorage.getItem('theme')
    if (stored === 'dark' || (!stored && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
      document.documentElement.classList.add('dark')
    }
  }, [])
{{/IF_DESIGN_DARK_MODE}}

  return (
    <ToastProvider>
      {/* TODO: Add other providers here (e.g., QueryClientProvider, AuthProvider) */}
      {children}
    </ToastProvider>
  )
}
